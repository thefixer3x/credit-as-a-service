{
  "name": "@caas/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Credit-as-a-Service Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint --no-error-on-unmatched-pattern --no-warn-ignored \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.2.0",
    "typescript-eslint": "^8.0.0"
  }
}